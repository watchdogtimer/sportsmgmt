class HomeController < ApplicationController
  def index
    #@news_items = NewsItem.find(:all, :order => 'post_time DESC')
    #@league_items = LeagueItem.find(:all, :order => 'post_time DESC')
    #@league = League.find(1)
  end
end